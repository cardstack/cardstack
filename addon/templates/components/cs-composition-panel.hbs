{{#if tools.activeContentItem}}
  {{cs-active-composition-panel
    model=tools.activeContentItem.content
    fields=fields }}
{{else}}
  <section class="cs-content-info">
    <p class="content-title">
      No editable content
    </p>
  </section>
{{/if}}
