import Component from '@ember/component';
import layout from '../../templates/components/cardstack/error-card-isolated';

export default Component.extend({
  layout,
  classNames: ['cs-error-card'],
});
