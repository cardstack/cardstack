import Component from '@ember/component';
import layout from '../../templates/components/cardstack/application-card-isolated';

export default Component.extend({
  layout,
  classNames: ['cs-application-card'],
});
