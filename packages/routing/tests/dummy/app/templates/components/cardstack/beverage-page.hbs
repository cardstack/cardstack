<div class="beverage-title">{{content.title}}</div>
{{#if content.hasBubbles}}
  <div class="bubbles"></div>
{{/if}}

{{#with (meta-for-resource content) as |meta|}}
   <div class="branch">{{meta.branch}}</div>
{{/with}}