export interface CardDropConfig {
  sku: string;
  discordRole: string;
}

export interface Web3Config {
  network: string;
}

export interface WalletConnectConfig {
  bridge: string;
  clientURL: string;
  clientName: string;
}
