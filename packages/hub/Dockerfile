FROM node:8.6
WORKDIR /hub

# Install the docker client only
RUN curl "https://download.docker.com/linux/static/stable/x86_64/docker-17.09.0-ce.tgz" | tar -xz -C /usr/local/bin --strip-components=1 docker/docker
