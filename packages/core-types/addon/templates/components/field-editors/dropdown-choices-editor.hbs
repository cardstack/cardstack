<div class="{{concat field '-selector'}}">
  {{#power-select-multiple disabled=(not enabled)
                  selected=(get content field)
                  options=options
                  onchange=(action (mut option))
                  as |option|}}
    {{option.title}}
  {{/power-select-multiple}}
</div>