<Boxel::CardContainer
  class="auth-step"
  ...attributes
>
  {{#if @header}}
    <header class="auth-step__header">
      {{@header}}
    </header>
  {{/if}}
  {{#if @description}}
    <div class="auth-step__description">
      {{@description}}
    </div>
  {{/if}}
  <div>
    {{yield}}
  </div>
</Boxel::CardContainer>
