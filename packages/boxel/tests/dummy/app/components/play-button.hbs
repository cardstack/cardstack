<Boxel::IconButton
  {{did-insert this.setupAudio}}
  onclick={{perform this.playPause}}
  @class="play-button {{@class}} {{if this.isPlaying "is-playing"}}"
  @icon={{if this.isPlaying "pause-btn" "play-btn"}}
  @width={{@width}}
  @height={{@height}}
/>