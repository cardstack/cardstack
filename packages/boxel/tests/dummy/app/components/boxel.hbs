{{#let (element this.tag) as |Tag|}}
  {{!-- not sure if the tag or class is actually necessary... we will find out as we use this component. --}}
  <Tag class="boxel {{@class}}" data-test-boxel ...attributes>
    {{yield}}
  </Tag>
{{/let}}