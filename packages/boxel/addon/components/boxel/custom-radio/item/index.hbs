{{!--
anything that's used as a label does not have its semantics in a screenreader.
that seems ok, since you probably shouldn't make a form work as document hierarchy.
aria-labelledby seems friendlier to safari than the for element, but unsure about other browsers.
--}}
<label
  class={{cn
    "boxel-custom-radio-option"
    boxel-custom-radio-option--grid=(not @hiddenInput)
    boxel-custom-radio-option--disabled=@disabled
  }}
  ...attributes
>
  <Input
    @type="radio"
    @checked={{@checked}}
    disabled={{@disabled}}
    name={{@name}}
    class={{cn
      "boxel-custom-radio-option__input"
      boxel-custom-radio-option__input--hidden=@hiddenInput
      boxel-custom-radio-option__input--checked=@checked
    }}
    {{on "change" (optional @onChange)}}
  />
  {{yield}}
</label>