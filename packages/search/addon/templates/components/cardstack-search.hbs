{{#each items as |item|}}
  {{yield item}}
{{else}}
  {{yield to="inverse"}}
{{/each}}

{{#if search.isRunning}}
  <div class="cardstack-email-loading">Loading...</div>
{{/if}}

{{#if cursor}}
  <button class="load-more" {{action (perform search cursor) }}>Load More</button>
{{/if}}
