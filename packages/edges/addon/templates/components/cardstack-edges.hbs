{{#toolbar-manager
  class="cardstack-edges"
  topRightCornerBelongsTo="top"
  topLeftCornerBelongsTo="left"
  bottomLeftCornerBelongsTo="bottom"
  bottomRightCornerBelongsTo="bottom"
}}
  {{#each cardstackEdges.topLevelComponents as |componentName|}}
    {{component
      componentName
      edges=(hash launcher = (component "fixed-within-toolbars" top=0 right=0))
    }}
  {{/each}}

  {{yield}}
{{/toolbar-manager}}



{{cardstack-modal-target}}
