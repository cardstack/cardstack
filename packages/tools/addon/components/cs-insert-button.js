import Component from '@ember/component';
import layout from '../templates/components/cs-insert-button';

export default Component.extend({
  layout,
  tagName: 'button',
  classNames: ['cs-insert-button']
});
