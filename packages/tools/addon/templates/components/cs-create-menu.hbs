<header>Select Content Type</header>

<ul>
  {{#each availableTypes as |type|}}
    {{! template-lint-disable no-invalid-interactive }}
    <li onclick={{action "create" type}}>
      {{cs-humanize type.id}}
      <button>Add +</button>
    </li>
  {{/each}}
</ul>
