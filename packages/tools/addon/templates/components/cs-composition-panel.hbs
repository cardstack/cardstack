{{#if tools.activeContentItem}}
  {{#if tools.activeContentItem.model.isCardstackPlaceholder}}
    {{cs-placeholder-composition-panel
      model=tools.activeContentItem.model
      editingEnabled=tools.editing
    }}
  {{else}}
    {{cs-active-composition-panel
      model=tools.activeContentItem.model
      headerSectionFields=tools.headerSectionFields
      renderedFields=tools.activeFields
      modelFields=tools.modelFields
      openedFieldId=tools.openedFieldId
      highlightedFieldId=tools.highlightedFieldId
      detailedViewFieldId=tools.detailedViewFieldId
      openField=(action "openField")
      highlightField=(action "highlightField")
      detailField=(action "detailField")
      editingEnabled=tools.editing
    }}
  {{/if}}
{{else}}
  <section>
    <p>No editable content.</p>
  </section>
{{/if}}