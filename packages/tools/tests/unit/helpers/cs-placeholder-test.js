
import { csPlaceholder } from 'dummy/helpers/cs-placeholder';
import { module, test } from 'qunit';

module('Unit | Helper | cs placeholder');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = csPlaceholder([42]);
  assert.ok(result);
});

