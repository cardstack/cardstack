<LinkTo
  @route={{@route}}
  @model={{@model}}
  @class={{concat "mode-indicator mode-indicator--" @mode}}
  aria-label={{if @ariaLabel @ariaLabel (concat "close " @mode " mode")}}
  data-test-mode-indicator-link={{@mode}}
  data-test-mode-indicator
>
  {{@mode}} mode
</LinkTo>