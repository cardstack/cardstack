<div
  class="embedded-card card-boundary"
  data-test-embedded-card={{@card.canonicalURL}}
  data-test-embedded-card-mode={{@mode}}>
  <div class={{if (or (eq @mode "view") (eq @mode "layout")) "embedded-card--view-fields"}}>
    {{#each @fields as | field |}}
      <FieldRenderer @field={{field}} @mode={{@mode}} @loadedField={{@loadedField}}/>
    {{/each}}
  </div>
</div>

{{!-- <div class="collection collection--{{@format}}">
  <div class="collection__main">
    <div class="collection__img" style={{css-url "background-image" item.cover_art_medium}}>
    </div>
    <div class="collection__title-desc">
      <h3 class="collection__title">{{titleize item.title}}</h3>
      <div class="collection__artist">{{item.artist}}</div>
    </div>
  </div>
</div> --}}