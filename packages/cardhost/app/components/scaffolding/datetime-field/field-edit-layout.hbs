<BoxelTextField
  @mode="edit"
  @type="datetime-local"
  @cardId={{@card.name}}
  @label={{@card.csTitle}}
  @value={{this.fieldValue}}
  @key-up={{perform this.updateFieldValue}}
  @focus-out={{perform this.updateFieldValue}}
/>