<Form::BoxelTextField
  @mode="edit"
  @type="datetime-local"
  @id={{@card.name}}
  @label={{or @card.csTitle @card.name}}
  @value={{this.fieldValue}}
  @setValue={{perform this.updateFieldValue}}
/>