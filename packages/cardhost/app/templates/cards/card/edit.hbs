{{#if (and (not-eq this.routeInfo.mode "themer") (not-eq this.routeInfo.mode "schema"))}}
  <div class="card-renderer--top-edge-buttons">
    <Cta title="Leave edit mode" {{on "click" this.leaveEditMode}} @variant="primary" class="mode-indicator-button" data-test-top-edge-link="view">
      Edit Mode
    </Cta>
    <SaveButton @card={{this.model}} />
  </div>

  <CardhostLeftEdge />
{{/if}}

{{outlet}}