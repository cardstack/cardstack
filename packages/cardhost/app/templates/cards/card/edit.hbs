{{#if (and (not-eq this.routeInfo.mode "themer") (not-eq this.routeInfo.mode "schema"))}}
  <CardstackEdge @edge="top" @componentToShow={{component "cardhost-top-edge" mode=this.routeInfo.mode card=this.model}}>
    <InTopToolbar />
  </CardstackEdge>

  <CardstackEdge @edge="left" @componentToShow={{component "cardhost-left-edge"}}>
    <InLeftToolbar />
  </CardstackEdge>
{{/if}}

{{outlet}}