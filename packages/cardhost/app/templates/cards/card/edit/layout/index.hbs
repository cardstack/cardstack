<CardViewer @card={{this.model}} @mode="layout"/>

{{#if this.cardstackSession.isAuthenticated}}
  {{!-- <RightEdge
    @card={{this.model}}
    @cardSelected={{true}}
  >
    <div class="right-edge--section" data-test-appearance-section>
      <div class="right-edge--section-header " data-test-section-header>
        Appearance
      </div>
      <div class="right-edge--items-container">
        <div class="right-edge--item">
          <Dropdown
            @theme="cs-dark"
            @searchEnabled={{false}}
            @label="Choose a theme:"
            @options={{this.themerOptions}}
            @selected={{this.selectedTheme}}
            @setValue={{this.handleThemeChange}}
          />
        </div>
        <div class="right-edge--item">
          <Cta
            {{on "click" this.createTheme}}
            @variant="secondary responsive"
            @theme="cs-dark"
            data-test-card-custom-style-button
          >
            New Custom Style
          </Cta>
        </div>
      </div>
    </div>
  </RightEdge> --}}

  <CardstackEdge
    @edge="right"
    @componentToShow={{component "right-edge"
      mode="layout"
      card=this.model
      cardSelected=true
      themerOptions=this.themerOptions
      selectedTheme=this.selectedTheme
      handleThemeChange=this.handleThemeChange
      createTheme=(action this.createTheme)
    }}>
    <InRightToolbar/>
  </CardstackEdge>
{{/if}}