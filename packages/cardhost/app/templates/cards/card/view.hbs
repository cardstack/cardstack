<CardhostTopEdge @mode="view" />

{{#if this.cardstackSession.isAuthenticated}}
  <div class="card-renderer--top-edge-buttons">
    <SaveButton @card={{this.model}} />
  </div>
{{/if}}

<CardhostLeftEdge />

<CardViewer @card={{this.model}} @mode="view" />