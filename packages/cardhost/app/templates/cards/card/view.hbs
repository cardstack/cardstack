<CardstackEdge @edge="top" @componentToShow={{component "cardhost-top-edge" mode="view" card=this.model}}>
  <InTopToolbar />
</CardstackEdge>

<CardstackEdge @edge="left" @componentToShow={{component "cardhost-left-edge"}}>
  <InLeftToolbar />
</CardstackEdge>

<CardViewer @card={{this.model}} @mode="view" />