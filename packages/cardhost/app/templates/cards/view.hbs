<CardhostTopEdge>
  <ModeSwitcher @name={{this.model.name}} @mode="view" />
  {{!-- <div>
    <LinkTo
      @route="cards.schema"
      @model={{this.model.name}}
      class="ch-button ch-button--secondary"
    >
      Update Schema
    </LinkTo>
    <LinkTo
      @route="cards.edit"
      @model={{this.model.name}}
      class="ch-button ch-button--secondary"
    >
      Edit Card
    </LinkTo>
  </div> --}}
</CardhostTopEdge>

<CardhostLeftEdge />

<div class="cards-view" data-test-card-view={{this.model.name}}>
  <CardRenderer @card={{this.model}} @format="isolated"/>

  <section class="cardhost-card-document">
    <h3>Card Document</h3>
    <CodeBlock
      @code={{this.cardJson}}
      @language="javascript"
    />
  </section>
</div>