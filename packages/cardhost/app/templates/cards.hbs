<div class="
  cardhost-cards
  {{if this.draggable.isDragging "dragging"}}
  {{this.routeInfo.mode}}
  {{if (or (eq this.mode "add") (eq this.mode "adopt")) "schema"}}
  {{if (or (eq this.mode "layout") (eq this.mode "fields")) "edit"}}
  {{if this.cardstackSession.isAuthenticated "logged-in" "logged-out"}}
  {{this.themerClasses}}"
  data-test-cardhost-cards
>
  <ToolbarManager @topLeftCornerBelongsTo="left" @topRightCornerBelongsTo="top" @animationDuration={{this.duration}}>
    {{outlet}}
  </ToolbarManager>
</div>

{{#unless this.hideTopEdge}}
  {{in-top-toolbar show=(
    component "cardhost-top-edge"
    mode=this.mode
    card=this.target.currentRoute.parent.attributes
    previousTransition=this.previousTransition
  )}}
{{/unless}}

{{#unless this.hideLeftEdge}}
  {{in-left-toolbar show=(component "cardhost-left-edge" mode=this.mode)}}
{{/unless}}

{{#if this.library.visible}}
  {{in-bottom-toolbar show=(component "library" templates=this.model)}}
{{/if}}