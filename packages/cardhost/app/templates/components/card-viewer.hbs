<section class="card-view" data-test-card-view={{@card.name}}>
  <div class="card-renderer--container" role="button" {{on "mouseenter" (fn this.setSelected true)}} {{on "mouseleave" (fn this.setSelected false)}} data-test-card-renderer>
    <CardRenderer
      @card={{@card}}
      @mode={{@mode}}
      @format="isolated"
      @cardSelected={{this.isSelected}}
      @contextMenuOpen={{this.contextMenuOpen}}
      @setContextMenu={{this.setContextMenu}}
    />
  </div>
</section>

<style data-test-view-css>
  {{scope-css @card.isolatedCss @card.name}}
</style>

{{!-- This is hidden, used in tests --}}
<section class="cardhost-card-document">
  <h3>Card Document</h3>
  <code data-test-card-json>{{this.cardJson}}</code>
</section>