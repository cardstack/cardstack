<Cta
  @variant="primary"
  class={{concat
    "save-button "
    (if this.justSaved "just-saved ")
    (if (not @card.isDirty) "saved ")
  }}
  data-test-card-save-btn
>
  {{#if @card.isDirty}}
    Save
  {{else}}
    Saved {{svg-jar "checkmark" width="12px" height="9px"}}
  {{/if}}
</Cta>