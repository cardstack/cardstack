<h3>Card Fields:</h3>
<dl>
  {{#each this.fields as | field |}}
    <div data-test-card-inspector-field={{field.fieldName}}>
      <dt>
        <strong><code>{{field.fieldName}}</code></strong>
        <pre data-test-card-inspector-value>{{field.fieldValue}}</pre>
      </dt>
      <dd>field type: <code data-test-card-inspector-field-type>{{field.fieldSchema.field-type}}</code></dd>
      <dd>is metadata field: <code data-test-card-inspector-is-meta>{{if field.fieldSchema.is-metadata "true" "false"}}</code></dd>
      <dd>needed when embedded: <code data-test-card-inspector-embedded>{{if field.fieldSchema.needed-when-embedded "true" "false"}}</code></dd>
    </div>
    <br>
  {{/each}}
</dl>