<fieldset class="field-creator--add-field">
  <legend>Add Field</legend>
  <label for="new_field_type">Field Type:</label>
  <select id="new_field_type" {{on "input" (action this.fieldTypeChanged)}}>
    {{#each this.fieldTypes as |fieldType|}}
      {{#if (eq this.newFieldType fieldType)}}
        <option selected>{{fieldType}}</option>
      {{else}}
        <option>{{fieldType}}</option>
      {{/if}}
    {{/each}}
  </select>
  <br>

  <label for="new_field_name">Field Name:</label>
  <Input
    @id="new_field_name"
    @value={{this.newFieldName}}
    @placeholder="e.g. Title"
  />
  <br>

  <Input
    @checked={{this.newFieldEmbedded}}
    @type="checkbox"
    @id="new_field_embedded"
  />
  <label for="new_field_embedded">Needed when Embedded</label>
  <br>

  <div class="field-creator--field-btn-wrapper">
    {{#if (gt @numFields 0)}}
      <label for="new_field_pos">position (0 based):</label>
      <Input
        @id="new_field_pos"
        @type="number"
        @min="0"
        @max={{@numFields}}
        @value={{this.newFieldPosition}}
      />
    {{/if}}

    <button {{on "click" this.addField}} class="ch-button" data-test-field-creator-add-field-btn>Add</button>
  </div>
</fieldset>