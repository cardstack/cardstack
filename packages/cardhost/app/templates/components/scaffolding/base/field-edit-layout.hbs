{{#unless this.load.isRunning}}
  <TextField
    @id={{concat "edit-" @card.name "-field-value"}}
    @label={{@card.csTitle}}
    @helperText={{this.fieldInstructions}}
    @value={{this.fieldValue}}
    @setValue={{perform this.updateFieldValue}} />
{{/unless}}