<dialog class="dialog {{@mode}}" tabindex="0" data-test-dialog-box {{autofocus}} ...attributes>
  {{#if @title}}
    <h1 class="dialog--title">
      {{svg-jar @icon width="20px" height="20px"}} {{@title}}
    </h1>
  {{/if}}
  <div class="dialog--content">
    {{yield}}
  </div>
</dialog>