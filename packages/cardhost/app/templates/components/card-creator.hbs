<h2 data-test-card-creator>Add Card:</h2>
<p>
  <em><code>HUB_ENVIRONMENT=test</code> environment variable must be set on the hub instance in order to provide session that allows this test to write to the hub.</em>
</p>

[[TODO add controls to add/remove metadata fields...]]<br>

{{#if this.statusMsg}}
  <div data-test-card-creator-msg><strong>{{this.statusMsg}}</strong></div>
{{/if}}

<CardInspector @card={{this.cardData}}></CardInspector>

<div>
  <button {{on "click" this.create}} data-test-card-creator-add-btn>Add Card</button>
</div>
<br>

<div>
  Card package name:
  <Input @value={{this.packageName}}></Input>
</div>
<div>
  Card instance identifier:
  <Input @value={{this.cardId}}></Input>
</div>

<IvyCodemirror
  @value={{this.card}}
  @valueUpdated={{action (mut this.card)}}
  @options={{hash
    lineNumbers=true
    mode="javascript"
  }}
>
</IvyCodemirror>