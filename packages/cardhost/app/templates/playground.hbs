<h1>Basic Card Chooser</h1>

{{!--
# NEEDS:
- List to iterator over
- If one is currently selected
- onChange
- Embeded card view

 --}}
 <div style="width: 400px">
  <Boxel::CardChooser
    @type="user"
    @onFetchCardsTask={{this.queryUsersTask}}
    @selected={{this.selected}}
    @onChange={{this.onChange}}
  />
 </div>