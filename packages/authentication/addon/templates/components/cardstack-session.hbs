{{yield
  (hash
    isAuthenticated=cardstackSession.isAuthenticated
    isPartiallyAuthenticated=cardstackSession.isPartiallyAuthenticated
    partialAuthenticationMessage=cardstackSession.partialAuthenticationMessage
    user=cardstackSession.user
    logout=(action "logout"))
}}
