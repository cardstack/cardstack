<Freestyle::Usage @slug="participants-box">
  <:example>
    <Boxel::ParticipantsBox @addAction={{this.addAction}}>
      <:list>
        <Boxel::ParticipantList
          @participants={{this.sampleParticipantGroup}}
        />
      </:list>
    </Boxel::ParticipantsBox>
  </:example>
  <:api as |Args|>
    <Args.Action
      @value={{this.addAction}}
      @defaultValue="null"
      @description="When passed, the `Add Participant` button is shown"
    />
    <Args.Yield
      @description="The contents of the box, typically one or more `Boxel::ParticipantsList` components"
    />
  </:api>
</Freestyle::Usage>

<Freestyle::Usage @slug="participants-box-no-action" @description="Without addAction passed">
  <:example>
    <Boxel::ParticipantsBox>
      <:list>
        <Boxel::ParticipantList
          @participants={{this.sampleParticipantGroup}}
        />
      </:list>
    </Boxel::ParticipantsBox>
  </:example>
</Freestyle::Usage>

<Freestyle::Usage @slug="participants-box-multi" @description="Displaying multiple participant groups">
  <:example>
    <Boxel::ParticipantsBox @addAction={{this.addAction}}>
      <Boxel::ParticipantList
        @participants={{this.sampleParticipantGroup}}
      />
      <Boxel::ParticipantList
        @participants={{this.sampleParticipantGroup2}}
      />
    </Boxel::ParticipantsBox>
  </:example>
</Freestyle::Usage>
