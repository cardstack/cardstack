<div class="catalog tools-preview">
  <header class="tools-preview--header">
    <h1 class="tools-preview--title">Syndication View</h1>
    <button {{action "edit"}} class="tools-preview--exit-button">
      Exit Preview
    </button>
  </header>

  <div class="tools-preview--grid">
    <Article @content={{this.model}} @format="thumbnail" />
    <Article @content={{this.model}} @format="embedded" />
    <Article @content={{this.model}} @format="isolated" />
  </div>
</div>