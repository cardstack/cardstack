exports.type = '@cardstack/core-types::string';

exports.compute = async function(model) {
  let thoughts = await model.getRelated('thoughts');
  if (!thoughts.length) { return; }

  let value = await thoughts[0].getField('description');
  return value;
}