exports.type = '@cardstack/core-types::string';

exports.compute = async function(model) {
  let toy = await model.getRelated('favorite-toy');
  if (!toy) { return; }

  let value = await toy.getField('toy-name');
  return value;
};