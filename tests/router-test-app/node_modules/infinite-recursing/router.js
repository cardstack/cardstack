module.exports = function(cardContext) {
  let { data: { id, type } } = cardContext;
  return [
    {
      path: '/omg',
      query: {
        filter: {
          type,
          id: { exact: id }
        }
      }
    }
  ]
};