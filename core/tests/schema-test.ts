import QUnit from 'qunit';
const { module, test } = QUnit;

import { Schema, CardLoader } from '@cardstack/core';

module('CardSchema', function () {
  test('empty card adopts from base card', async function (assert) {

  });
});
